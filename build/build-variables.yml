# Repo: FirelyTeam/firely-net-sdk
# File: build/build-variables.yml
# Variables used during builds.

variables:
  NET_CORE_SDK: 5.0.x
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  TEST_TARGETFRAMEWORK: net5.0
  buildConfiguration: Release